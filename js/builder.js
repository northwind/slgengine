/**
 * 程序主入门
 */
$(function(){
	
	if ( $.browser.msie )
		return false;
		
	PANEL = new Panel();
	
/*
	PANEL.on( "start", function( panel ){
		PANEL.moveWinTo( 0,1000 );
		PANEL.showGrid();

	} );
*/
		
 });